{
  "name": "swivel",
  "version": "4.0.3",
  "description": "Message passing between ServiceWorker and pages made simple",
  "main": "swivel.js",
  "author": "Nicolas Bevacqua <nicolasbevacqua@gmail.com> (http://bevacqua.io/)",
  "license": "MIT",
  "scripts": {
    "build": "browserify -s swivel -do dist/swivel.js swivel.js && uglifyjs -m -c -o dist/swivel.min.js dist/swivel.js",
    "deploy": "npm run build && npm run deployment",
    "deployment": "git add dist && npm version ${BUMP:-\"patch\"} --no-git-tag-version && git add package.json && git commit -am \"Autogenerated pre-deployment commit\" && git reset HEAD~1 && git add . && git commit -am \"Release $(cat package.json | jq -r .version)\" && git push --tags && npm publish && git push"
  },
  "dependencies": {
    "atoa": "1.0.0",
    "contra": "1.9.1"
  },
  "devDependencies": {
    "browserify": "12.0.0",
    "uglify-js": "2.5.0"
  }
}
